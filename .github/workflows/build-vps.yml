name: Build VPS binary

on:
  workflow_dispatch:
  push:
    branches:
      - lenard/mail-workspace

permissions:
  contents: read

concurrency:
  group: build-vps-${{ github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always

jobs:
  build:
    name: Linux x86_64
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-unknown-linux-gnu
      - uses: Swatinem/rust-cache@v2
        with:
          key: vps-x86_64
      - uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: apps/web/package-lock.json

      - name: Install system dependencies
        run: sudo apt-get update && sudo apt-get install -y libssl-dev libdbus-1-dev pkg-config

      - name: Build web app
        working-directory: apps/web
        run: npm ci && npm run build

      - name: Build MXR
        run: cargo build --release --locked --target x86_64-unknown-linux-gnu -p mxr --no-default-features --features personal

      - name: Package binary
        run: |
          mkdir -p dist
          cp target-cli/x86_64-unknown-linux-gnu/release/mxr dist/mxr
          cd dist
          sha256sum mxr > mxr.sha256

      - name: Smoke binary
        run: dist/mxr --version

      - uses: actions/upload-artifact@v7
        with:
          name: mxr-linux-x86_64-${{ github.sha }}
          path: dist/
          retention-days: 7
